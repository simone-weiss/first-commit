{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Hugo Server",
            "type": "shell",
            "command": "cd site && sudo hugo server --bind 0.0.0.0 --disableFastRender",
            "problemMatcher": [],
            "isBackground": true
        }
    ]
}